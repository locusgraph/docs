#!/bin/sh
# Enable with: git config core.hooksPath .githooks

git diff --name-only ORIG_HEAD HEAD 2>/dev/null | grep -q '^pnpm-lock\.yaml$' &&
  echo "post-merge: pnpm-lock.yaml moved — run pnpm install before trusting a typecheck"

exit 0
